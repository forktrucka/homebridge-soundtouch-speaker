# Supply-chain hardening: do not auto-run lifecycle scripts (preinstall/
# install/postinstall) from dependencies during `npm install`.
# Explicitly invoked scripts (e.g. `npm run prepare`) still run.
ignore-scripts=true
